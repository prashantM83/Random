<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slip2Intent">

        <!-- Program 1: Intent Data Passing — Sender (Main Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Prog1: Intent Sender">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Program 1: Intent Receiver -->
        <activity
            android:name=".SecondActivity"
            android:exported="false"
            android:label="Intent Receiver" />

        <!-- Program 2: Image Change -->
        <activity
            android:name=".ImageChangeActivity"
            android:exported="true"
            android:label="Prog2: Image Change">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
