<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.practical5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practical5">

        <!-- 5a: Menus -->
        <activity android:name=".MainActivityMenus" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 5b: Date/Time Picker Dialogs -->
        <activity android:name=".MainActivityDialog" android:exported="false" />

        <!-- 5c: Dialog Fragments -->
        <activity android:name=".MainActivityDialogFragment" android:exported="false" />

    </application>

</manifest>
